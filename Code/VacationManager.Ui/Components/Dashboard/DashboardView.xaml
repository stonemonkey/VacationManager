<UserControl x:Class="VacationManager.Ui.Components.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DockPanel>
        <Expander Header="{Binding VacationDays.DisplayName}" IsExpanded="True" HorizontalAlignment="Left" DockPanel.Dock="Top">
            <ContentControl x:Name="VacationDays"/>
        </Expander>
        <Expander Header="{Binding PendingRequests.DisplayName}" IsExpanded="True" HorizontalAlignment="Left" DockPanel.Dock="Left">
            <ContentControl x:Name="PendingRequests"/>
        </Expander>
    </DockPanel>
</UserControl>
